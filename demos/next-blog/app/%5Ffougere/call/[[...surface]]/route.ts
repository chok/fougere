/**
 * The call envelope. The optional catch-all is what lets `/_fougere/call/public`
 * select a named surface — `surfaceOf` reads the segment in `@fougere/app`.
 */
export { POST } from '@fougere/next/call';
