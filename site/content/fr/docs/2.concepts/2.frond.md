---
title: La Frond
description: La frontière de propriété — ce qu'une Frond porte, et les six couches du modèle.
---

# La Frond

Le citoyen de première classe de Fougere n'est ni le transport, ni la page, ni la table —
c'est la **Frond** : un module métier avec une frontière de propriété. Le nom vient de la
fronde de la fougère : la même forme à toutes les échelles.

## La frontière de propriété

Une Frond possède, et rien d'autre ne possède à sa place :

- ses **entités** et schémas,
- ses **opérations** — commands et queries ([handlers](/fr/docs/business/handlers)),
- ses **policies** — qui peut faire quoi, jugé dans les opérations,
- ses **events** — les faits que ses commands produisent,
- ses **adapters de persistance** — comment ses entités se réalisent,
- ses **contrats publics** — ce que les autres Fronds peuvent appeler,
- ses **règles d'invalidation** — ce qu'une command revalide.

Deux règles en découlent. Une Frond **n'importe pas les entrailles d'une autre** — les
interactions passent par des contrats explicites, de préférence opérations et events,
jamais par des fouilles internes. Et une **App compose des Fronds** — elle ne doit pas
devenir l'endroit où le domaine est reconstruit à coups de pages, fetchs, stores et
endpoints.

## Les six couches

Le modèle se lit comme un pipeline — chaque couche dérivée de la précédente :

| # | Couche | Ce qui s'y passe |
|---|---|---|
| 1 | **Déclaration** | l'auteur déclare entités, vues, opérations, events — ni HTTP ni SQL ici |
| 2 | **Dérivation** | validation, types, vues io, metadata lisible par les adapters en dérivent |
| 3 | **Capacité locale** | la déclaration devient exécutable in-process — le mode de référence |
| 4 | **Projection** | des façades sont générées vers les transports : HTTP, GraphQL, CLI, workers |
| 5 | **Composition** | les Fronds sont scannées et reliées dans un runtime — module → contrat → interaction |
| 6 | **Distribution** | une capacité part hors process ; sérialisation, timeouts, retries apparaissent **ici**, explicitement |

La distribution change la topologie, jamais la modélisation métier — c'est le
[gradient](/fr/docs/infra/gradient).

## Généré vs écrit à la main

La frontière est stricte :

| Fougere génère | Vous écrivez |
|---|---|
| validation, vues dérivées, tables/metadata | la logique métier non triviale |
| types et inputs GraphQL, CRUD trivial | les invariants riches, les workflows multi-étapes |
| les façades de projection (contrat → transport) | les politiques d'erreur, les décisions de cohérence |
| les clients typés, les manifests de composition | l'orchestration inter-Fronds sensible |

La règle en une ligne : **Fougere génère l'infrastructure répétitive autour d'un
contrat ; le développeur écrit le comportement derrière.** Pas de couche service
obligatoire — un handler va de « fait tout » à « façade mince » selon ce que le domaine
exige.

## Dans le système de fichiers

```
fronds/blog/
  package.json          ← { "fougere": { "frond": "blog" } }
  entities/             ← les déclarations
  handlers/             ← les opérations
  collectors/           ← la résolution de paramètres par type
  seeds/                ← la donnée de boot par les opérations jugées
```

Tout est découvert par scan — voir [Démarrer](/fr/docs/getting-started).
