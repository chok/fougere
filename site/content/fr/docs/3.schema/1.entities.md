---
title: Entités
description: Le vocabulaire des champs, les quatre axes, et Entity.validate.
---

# Entités

Une entité est une classe qui étend la factory `entity()`. Elle fournit le type
TypeScript, la validation et les métadonnées lues par les adapters.

```ts
import { entity, primary, text, ref, created, oneOf, date, readOnly, optional } from '@fougere/schema';
import Author from './Author.js';

export default class Post extends entity({
  id: primary(),
  slug: text({ min: 1, max: 80 }),
  title: text({ min: 1, max: 160 }),
  body: optional(text()),
  authorId: ref(Author),
  createdAt: created(),
  status: readOnly(oneOf('draft', 'published', { default: 'draft' })),
  publishedAt: readOnly(optional(date())),
}) {}
```

Le nom de classe sert d'identité : `Post.name` nomme la table SQLite, le type GraphQL, la
clé d'enregistrement (`post`) et le match DI. `entity()` ne prend pas de nom séparé.

## Le vocabulaire des champs

Champs de valeur :

| Helper | Type | Options |
|---|---|---|
| `text(opts?)` | `string` | `min`, `max`, `pattern`, `format`, `default` |
| `email(opts?)` | `string` | options de `text` sans `format` |
| `url(opts?)` | `string` | options de `text` sans `format` |
| `number(opts?)` | `number` | `min`, `max`, `integer`, `default` |
| `bool(opts?)` | `boolean` | `default` |
| `date()` | `Date` | — |
| `oneOf(...valeurs, opts?)` | union de littéraux | `default` |
| `list(item, opts?)` | `T[]` | item = n'importe quel champ |

Champs de rôle :

| Helper | Sens |
|---|---|
| `primary()` | clé primaire, générée — s'applique aussi à un champ : `primary(text())` |
| `ref(Entity)` | clé étrangère (`string`) ; accepte `() => Entity` pour les cycles |
| `many(Entity)` | one-to-many — rôle seul, pas de colonne |
| `unique(f)` | deux lignes ne portent pas la même valeur — une contrainte que la **base** applique |
| `indexed(f)` | les lectures filtrent souvent dessus — émet `CREATE INDEX`, ne change aucune réponse |

Les règles `oneOf`, `min` et `max` sont aussi émises en contraintes `CHECK`. Elles
s'appliquent donc aux écritures qui ne passent pas par la façade, par exemple une requête
SQL directe ou un autre processus. `pattern` et `format` restent validés à la façade, car
les dialectes d'expressions régulières diffèrent selon les bases.

Certains faits portent sur une paire, pas sur un champ. « Un livre apparaît une fois
dans une liste » est vrai de `(listId, docId)` et d'aucun des deux seul, donc il se
déclare sur l'entité :

```ts
class ListBook extends entity({
  id: primary(),
  listId: ref(List),
  docId: text(),
}, {
  unique: [['listId', 'docId']],
}) {}
```

La base applique cette contrainte afin de couvrir aussi les écritures concurrentes. Une
dérivation qui retire un membre du groupe retire également le groupe : conserver seulement
`(listId)` changerait la règle déclarée.

Champs de lifecycle :

| Helper | Sens |
|---|---|
| `created()` | tamponné à la création (`createdAt`) — jamais écrit par un client |
| `updated()` | re-tamponné à chaque mise à jour (`updatedAt`) |

Wrappers (se composent autour de n'importe quel champ) :

| Wrapper | Axe | Effet |
|---|---|---|
| `optional(f)` | shape | peut être absent ; `T \| null` |
| `nullable(f)` | shape | peut être `null`, doit être présent |
| `immutable(f)` | lifecycle | écrivable à la création, interdit en mise à jour |
| `readOnly(f)` | boundary | ne traverse jamais vers l'intérieur — sortie seule |
| `writeOnly(f)` | boundary | ne traverse jamais vers l'extérieur — entrée seule (mots de passe) |

Les wrappers s'emboîtent : `readOnly(optional(date()))` est une date possédée par le
serveur, possiblement absente.

> `Post.validate()` ne voit que l'entrée courante et ne peut donc pas vérifier l'unicité
> parmi les lignes existantes. La base applique cette règle et renvoie alors une erreur de
> driver plutôt qu'une erreur de champ. Un champ `primary()` est déjà unique.

### Les deux écritures, deux propriétaires

`unique(slug)` déclare une contrainte sur un champ ; `unique: [['listId','docId']]` en
déclare une sur plusieurs. Ce ne sont pas le même énoncé, donc ils ne vivent pas au même
endroit : une contrainte sur un champ appartient à ce champ, une contrainte couvrant
plusieurs champs appartient au schéma — elle n'est à aucun d'eux.

Au runtime, un champ porte `role.unique`, un booléen, lu par `Role.of(field).isUnique`. Un
groupe couvrant plusieurs champs est tenu une fois par le schéma, et `getUnique()` le rend.

La carte aplatit les deux sur les membres, parce qu'un lecteur dans un autre langage voit
un champ à la fois et doit pouvoir reconstruire la contrainte depuis ce que ce champ
porte :

```jsonc
// describe(ListBook) — le `role.unique` de chaque membre
"listId": { "role": { "unique": [["listId", "docId"]] } },
"docId":  { "role": { "unique": [["listId", "docId"]] } }
```

Un champ appartenant à deux contraintes porte deux entrées. Chaque membre porte le groupe
entier, ce qui permet de reconstruire une contrainte composée à partir de la seule carte.

Une dérivation qui retire un membre retire le groupe : `Post.pick('listId')` ne conserve
pas la contrainte composée.

### Où une référence atterrit

`ref(Entity)` nomme une cible, pas un endroit. La cible peut être l'entité d'une autre
Frond, atteinte par le nom que cette Frond publie — jamais par un chemin relatif, que
`fougere check` signale en `cross-frond-import` :

```ts
// fronds/blog/entities/Post.ts
import User from '@fronds/user/entities/User.js';

authorId: readOnly(ref(User)),
```

La déclaration est la même où que se trouvent finalement les lignes de la cible. Ce qui
change, c'est qui la tient — et le boot le décide par PAIRE, jamais par moteur :

| Les lignes de la cible | Qui la tient | Ce que ça coûte |
|---|---|---|
| la même source | la clé étrangère, `references "users" ("id")` | rien |
| une autre [source](/fr/docs/infra/sources) | le garde du storage, qui lit la ligne avant l'écriture | une lecture par écriture |
| un autre process | personne, et le boot le dit au démarrage | une ligne peut nommer ce qui n'existe plus |

Une clé étrangère est une promesse que fait une base, donc elle s'arrête à cette base :
deux bases ne partagent aucune contrainte. Ce qui ne s'arrête pas, c'est la promesse
elle-même, et c'est la raison de la deuxième ligne — le garde demande au storage de la
cible si la clé est là, et refuse l'écriture sinon. Ce n'est pas la même garantie : la cible
peut être supprimée entre la lecture et l'écriture. Elle attrape ce qu'une clé attrape en
pratique, c'est-à-dire une clé qui n'a jamais été bonne — une ligne copiée d'un autre
environnement, un utilisateur supprimé il y a des mois, un id de test.

La troisième ligne est celle que rien ne peut tenir, alors elle est annoncée :

```
WRN [relations] authorId → user — declared, and nothing in this process holds them:
    no foreign key, and the target answers elsewhere. A row may name one that is gone.
```

### Ce qu'une ligne devient quand celle qu'elle nomme s'en va

`ref(Entity, { onDelete })` le dit, et ne rien dire veut dire `restrict` — une clé étrangère
refuse déjà une suppression qui laisserait une ligne orpheline.

| | |
|---|---|
| `restrict` | la suppression est refusée tant qu'une ligne la nomme |
| `cascade` | les lignes s'en vont avec elle |
| `set null` | les lignes restent, le champ est vidé — refusé au boot sur un champ qui n'admet pas de null |

Qui l'exécute suit le même tableau que ci-dessus : le moteur là où une clé le tient, le
framework sinon. Le framework parcourt l'arbre **du bas vers le haut** et retire la ligne en
dernier, si bien qu'une interruption laisse moins d'enfants et jamais un orphelin — c'est ce
qui permet à une libération de traverser un process sans validation en deux phases. Il n'y a
rien à annuler, seulement quelque chose à finir.

Un seul saut décide pour tout l'arbre. Une cascade exécutée par un moteur ne passe jamais par
le framework : un saut que le moteur possède au-dessus d'un saut qu'il ne possède pas
retirerait des lignes en laissant derrière celles qui les nomment. Un seul saut non tenu plus
bas, et le framework prend l'arbre entier.

Ce qui finit une libération interrompue, c'est [`@fougere/workflow`](/fr/docs/infra/lifecycle) :
son journal est une entité, et un run dit que quelqu'un a commencé. Sans le paquet, les sauts
sont les mêmes ; ce qui s'arrête s'arrête.

## Les quatre axes

Chaque champ porte quatre axes orthogonaux — c'est le modèle que tous les adapters lisent :

| Axe | Question | Lu par |
|---|---|---|
| **shape** | quelles valeurs sont valides ? (le shape *est* du JSON Schema) | validation, formulaires, colonne SQL, scalar GraphQL |
| **role** | quel rôle joue-t-il ? (`primary`, `ref`, `many`, `unique`) | DDL (PK/FK, contraintes), relations, carte |
| **lifecycle** | qui l'écrit, quand ? (`create: 'now' \| 'optional' \| {value} \| {generate}`, `update: 'now' \| 'forbidden'`) | réalisation storage, détente du constructeur |
| **boundary** | dans quel sens traverse-t-il l'API ? (`readOnly`, `writeOnly`) | `Visibility.input` / `Visibility.output` |

La façade valide l'entrée client avec les axes `shape` et `boundary` : écrire un champ
`readOnly` comme `status` est refusé. Le storage applique ensuite l'axe `lifecycle`, par exemple
en renseignant `createdAt` et les valeurs par défaut.

> La validation refuse les clés inconnues avec `Unknown field` au lieu de les retirer.
> Une entrée acceptée correspond donc exactement au contrat (voir
> [Handlers](/fr/docs/business/handlers#les-clés-inconnues-sont-refusées)).

## Entity.validate

Le moteur de validation est edge-safe (`@cfworker/json-schema`). La même fonction peut
être utilisée dans le navigateur et dans le handler :

```ts
Post.validate(input)
// → { success: true, data }                                    valide
// → { success: false, errors: [{ path: 'title', message }] }   invalide, par champ
```

`new Post(data)` construit une instance sans validation. Les champs générés ou automatiques
sont optionnels pour le constructeur, mais restent présents dans le type résultant.

Suite : [Vues](/fr/docs/schema/views) — dériver les contrats depuis le schéma.
