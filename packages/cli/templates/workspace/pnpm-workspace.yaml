packages:
  - fronds/*
  - apps/*

allowBuilds:
  better-sqlite3: true
  esbuild: true

# A project is created the day a version is published: the delay pnpm can apply to a fresh
# release would hold back the very version the scaffold was written for.
minimumReleaseAgeExclude:
  - '@fougere/*'
