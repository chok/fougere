export default {
  operations: {
    // Its name deliberately carries no convention; the test needs the custom-operation
    // mutation shape shared by the GraphQL door harness.
    inspect: { kind: 'command' },
  },
};
