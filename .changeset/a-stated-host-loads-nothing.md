---
'@fougere/app': patch
---

A host that states its fronds and hands its config over reads no source, so it installs no
module loader. The guard counted only a scan: the Nuxt app of `demos/cloudflare-d1` built
jiti under workerd and answered 500 to every call.
