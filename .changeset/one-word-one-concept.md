---
'@fougere/schema': minor
---

`Formats` is `StringFormats`, because `Format` now names what writes a JSON Schema.

The registry holds the predicates behind the `format` keyword — `slug`, `siret` — and the word
was doing two jobs since `Format.of(…)` arrived.
